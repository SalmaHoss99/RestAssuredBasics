name: Continous Integration

on:
  push:
    branches:
      [master]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run: echo "build"

      - name: test
        run: echo "test"

      - name: deploy
        run: echo "deploy"